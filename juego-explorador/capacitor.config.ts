// capacitor.config.ts
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angie.juegoexplorador',
  appName: 'Juego Explorador',
  webDir: 'out',      
};

export default config;
